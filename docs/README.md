# docs/

`viewer.png` is a screenshot of the running viewer (`python -m web.server`,
then capture the page). `README.md` and `web/README.md` both point at this one
file, so replacing it updates both.
