# docs/

`viewer.png` is a **placeholder**. Replace it with a real screenshot of the
running viewer (`python -m web.server`, then capture the page); `README.md` and
`web/README.md` both point at this one file, so replacing it updates both.
